import { Widget } from "@/components/Widget";
import Image from "next/image";

export default function Home() {
	return (
		<main className="flex-1 flex justify-center items-center m-auto  self-center h-screen">
			<Widget />
		</main>
	);
}
